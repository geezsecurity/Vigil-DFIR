# Screenshots

Images used by the project `README.md`. All captured from the built-in sample case
(**Load sample**), so no real incident data is shown.

| File                 | View                                                                |
|----------------------|---------------------------------------------------------------------|
| `dashboard.png`      | Dashboard / overview (hero image).                                  |
| `detections.png`     | Detections tab: severity, attack chains, MITRE ATT&CK, entities.    |
| `entity-profile.png` | Entity risk dossier (per host/user/IP slide-out).                   |
| `timeline.png`       | Super-timeline (histogram + merged notable-event feed).             |
| `evidence.png`       | Evidence tab (mined forensic artifacts).                            |
| `rarity.png`         | Rarity / long-tail stacking (rare parent -> child ancestry).        |
| `events.png`         | Event grid with search / filter / columns.                          |
| `firewall.png`       | Firewall tab + event-detail panel (JSON + analyst verdict).         |
| `vpn.png`            | VPN tab + event-detail panel (kept as a spare; not in README).      |

To refresh a shot: load the sample case, open the tab, capture the browser content
(crop out OS chrome), and overwrite the file with the same name.
